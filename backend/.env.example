# Скопируйте в .env и подставьте свои значения.
# .env не отслеживается git и содержит локальные пути/порты.

# Внешний порт сервера на хосте (если занят — поменять здесь, коммитить не нужно)
HOST_PORT=8095

# Путь к медиатеке на хосте
MEDIA_PATH=/path/to/media
